<h3 style="text-align: center;">Index</h1>

<h1>My Tasks</h1>

<% @tasks.each do |task| %>
    <p>■ <%= link_to task.title, task %> <%= link_to "🗑", task, data: {turbo_method: :delete, turbo_confirm: "Are you sure?"} %></p>
<% end %>


<h5><%= link_to "Add new task", new_task_path %></h5>
